###

=> Data Types: 
    string(to store the any text type)
    int(to store integer/number)
    float(to store integer/number with decimal values)
    bool(to store true or false)

=> Operators: for mathemetical calculations(+, -, *, /, %) 

=> Conditional Statements (if, elif, else): descision making

=> Comparison Operators: to make the comparision (==, >, <, =>, =>, !=)


